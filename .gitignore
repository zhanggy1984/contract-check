# 本地配置（含 API key），禁止提交
**/.env
.env

# Python
__pycache__/
*.pyc
.venv/
venv/

# 上传文件 / OCR 临时文件
uploads/
tmp/
# data/ 只入库演示脚本与说明；PDF 由 data/gen_demo_contracts.py 本地生成（体积大不入库）
data/parsed/
data/**/*.pdf
data/**/*.docx
data/**/*.png
# backend 运行产生的数据目录（测试合同、解析产物、报告截图等，均不入库）
backend/data/

# 前端
node_modules/
dist/

# 部署启停脚本（本机一键脚本，无需提交）
start.ps1
start.sh
stop.ps1

# Claude Code 本地配置（skill 等，个人环境不入库）
.claude/
